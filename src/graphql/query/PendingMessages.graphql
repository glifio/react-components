query PendingMessages($address: String!) {
  pendingMessages(address: $address) {
    cid
    to {
      robust
      id
    }
    from {
      robust
      id
    }
    nonce
    value
    gasLimit
    gasFeeCap
    gasPremium
    method
    height
    params
  }
}
