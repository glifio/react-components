query PendingMessages($address: String!) {
  pendingMessages(address: $address) {
    cid
    from {
      id
      robust
    }
    to {
      id
      robust
    }
    nonce
    value
    gasLimit
    gasFeeCap
    gasPremium
    method
    params
  }
}
