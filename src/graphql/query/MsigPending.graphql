query MsigPending($address: String!) {
  msigPending(address: $address) {
    id
    to {
      id
      robust
    }
    method
    params
    value
    proposalHash
    approved {
      id
      robust
    }
  }
}
