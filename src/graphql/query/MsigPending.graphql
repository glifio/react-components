query MsigPending($address: String!) {
  msigPending(address: $address) {
    id
    to {
      id
      robust
    }
    value
    method
    params
    approved {
      id
      robust
    }
    value
    proposalHash
  }
}
