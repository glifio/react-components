query MsigPending($address: String!, $limit: Int!, $offset: Int!) {
  msigPending(address: $address, limit: $limit, offset: $offset) {
    id
    to {
      id
      robust
    }
    value
    method
    params
    approved {
      id
      robust
    }
    value
    proposalHash
  }
}
